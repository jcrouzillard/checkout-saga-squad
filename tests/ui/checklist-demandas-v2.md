# Checklist — Demandas v2: tipo + validação agêntica (D4 `1cc732c62a2d`, ADR-008)

> Branch `feature/D4-formulario-e-validacao-de-demanda`, worktree `plankton-d4` (não commitado).
> Contrato: `docs/contracts/ui-demandas-v2.md`. Servidor isolado `python3 tools/squad/server.py --port 7071`.
> Triagem real (`tools/squad/triage.py --runner claude`) executada com o Claude Code de verdade, sem
> mocks. Capturas reais: Chrome headless (`zenika/alpine-chrome`) e `ghcr.io/puppeteer/puppeteer`
> (clique programático em `[data-view="demandas"]`), ambos via `http://host.docker.internal:7071/`.
> Legenda: ✅ passou · ⚠️ parcial/validar · ❌ falhou.

| CA | Resultado | Evidência |
|----|-----------|-----------|
| CA1 — Tipo obrigatório, sem padrão; 400 sem tipo | ✅ | `curl -XPOST /api/demand -d '{"title":"x"}'` → `400 {"error":"tipo obrigatório: produto \| operacao"}`; idem para `kind` inválido. Código: `<fieldset class="kind"><legend>Tipo (obrigatório)</legend>` com 2 `<input type="radio" name="dem-kind">`, nenhum com `checked` (sem padrão). |
| CA2 — Evento `task` com `kind`; tag no card; label na issue | ✅ | Toda resposta `201` de `POST /api/demand` trouxe `"kind":"produto"`/`"operacao"` no evento; captura `demandas-d4-1440.png` mostra as tags `produto`/`Com perguntas`/`operação` nos cards. `tools/squad/github_sync.py:170-171` gera `label = f"tipo:{e['kind']}"` a partir do evento `task` (revisão de código; sem `gh issue view` ao vivo nesta rodada — sync real do GitHub não acionado para não gerar issues de teste). |
| CA3 — "Em validação" → "Validada"/"Com perguntas" em < 60s | ✅ | 4 triagens reais medidas (`date +%s` antes/depois de `triage.py`): demanda vaga `74a940381b6d` = **11s**; demanda "quase-duplicata" `c6d5ab024e57` = **15s**; demanda técnica `eda5679b5916` = **21s**; demanda simples `feb87d9f072e` = **10s**. Todas bem abaixo de 60s. |
| CA4 — Demanda vaga → `perguntas` (1–5, com `dimension`); demanda completa → `ok` | ✅ | Vaga ("melhorar o visual", sem descrição) → `status:"perguntas"`, 4 perguntas, uma por dimensão (`objetivo`,`escopo`,`aceite`,`restricoes`). Demanda simples e bem especificada ("header fixo X-App-Version") → `status:"ok"` ("Validação: pronta para iniciar"). **Observação**: uma 3ª demanda, com o texto completo da D1 (já implementada no repo), recebeu 1 pergunta pedindo para confirmar se não é duplicata da D1 — comportamento correto do Analista (ele lê o repo antes de perguntar), não um defeito. |
| CA5 — Perguntas no card + campo de resposta; `clarify` gera evento e é usado no inbox | ✅ | Captura `demandas-d4-1440-scroll.png`: card com "O Arquiteto tem 2 perguntas antes de iniciar", cada uma com tag da dimensão (`critério de aceite`) + `<textarea>` de resposta + botão "Enviar respostas". `POST /api/demand/clarify` com `validation` de **outra** demanda → `404 {"error":"demanda ou validação não encontrada"}`; com a `validation` correta e respostas → `201`, evento `clarification` gravado. Comentário na issue do GitHub (`github_sync.py`) não verificado ao vivo (sync não acionado nesta rodada). |
| CA6 — Iniciar bloqueado (UI + 409) até `ok`/respostas; override exige nota | ✅ | Testado ao vivo, na ordem: (1) `start` antes de `clarify` → `409 {"error":"demanda aguardando validação"}`; (2) `clarify` com `validation` de outra demanda → `404`; (3) `clarify` correto → `201`; (4) `start` da mesma demanda após `clarify` → `201`; (5) `start` de demanda **sem triagem** e sem override → `409`; (6) `override:true` sem `note` → `400 {"error":"override exige nota"}`; (7) `override:true` com `note` → `201`, evento com `"override":true`; (8) `start` de demanda com validação `ok` (sem clarify/override) → `201` direto. Captura mostra botão "Iniciar" desabilitado com `aria-describedby="why-{id}"` apontando para "Responda às perguntas do Arquiteto para liberar o início." e o checkbox "Iniciar mesmo assim (override registrado no log)". |
| CA7 — Inbox com `kind` e `clarifications`; roteamento por tipo | ✅ | `docs/squad/inbox/74a940381b6d.json` (fluxo com respostas): `"kind":"operacao"`, `"clarifications":[{question,dimension,answer}×4]`, `"override":false`. `docs/squad/inbox/2f7346fa6d5a.json` (override): `"kind":"operacao"`, `"clarifications":[]`, `"override":true`. `docs/squad/inbox/feb87d9f072e.json` (validação `ok`): `"kind":"produto"`, `"clarifications":[]`. Roteamento por tipo (Arquiteto para `produto`, Frontend/Orquestrador para `operacao`) é regra do plantão (`plantao.md`), não exercitado ao vivo (exigiria o vigia rodando) — confiado na leitura do prompt/contrato. |
| CA8 — Analista não altera arquivos; `pending.py` não relista | ✅ | `git status --porcelain` antes/depois das 4 triagens: só `docs/squad/memory/decisions.jsonl` modificado (nenhum arquivo de código tocado pelo Arquiteto em modo triagem). `pending.py` antes das triagens listou `validação: <id>` para as 3 demandas; depois das triagens, só a demanda **não triada** (`2f7346fa6d5a`, usada de propósito para o teste de override) continuou listada; rodado **duas vezes seguidas** com saída idêntica (sem duplicar nem relistar as já validadas). |
| CA9 — Sem regressão: demandas antigas (sem `kind`) continuam "Registrada"/iniciáveis | ✅ | Revisão de código: `validationInfo(d, related)` retorna `null` quando `!d.kind` (linha inicial da função); `demandInfo` usa `vinfo ? VSTATUS[vinfo.st] : "Registrada"` — sem `kind`, cai direto em `"Registrada"`. O cálculo do botão Iniciar é `const ready = !vinfo \|\| vinfo.ready` — sem `vinfo` (demanda antiga), `ready = true` sempre, ou seja, iniciável sem bloqueio, exatamente como antes da D4. Não havia demanda antiga sem `kind` nesta instância isolada do servidor (log criado do zero no worktree) para um teste 100% ao vivo; a garantia vem da leitura direta da lógica, que é inequívoca. |
| CA10 — Acessibilidade (fieldset/legend, label, aria-live, motivo do desabilitado) | ✅ | `grep`: `<fieldset class="kind"><legend>Tipo (obrigatório)</legend>` (1); rádios com `aria-describedby="dem-kind-help-{k}"` apontando pro texto de ajuda; `<label for="ans-{id}-{qid}">` em cada pergunta/resposta; região global `id="sr-live" role="status" aria-live="polite"` usada para anunciar mudança de estado das demandas (comentário no código confirma o uso); botão "Iniciar" desabilitado carrega `aria-describedby="why-{id}"` ligado a um `<p class="why" id="why-{id}">` com o motivo em texto (visível na captura). |

## Fluxo de teste real (resumo executável)

1. Servidor isolado: `python3 tools/squad/server.py --port 7071` (porta dedicada, sem conflito com o painel principal em 7070).
2. 4 demandas via `curl -X POST http://localhost:7071/api/demand`: vaga (`operacao`), quase-duplicata da D1 (`produto`), técnica com pequenos gaps (`produto`), e simples/completa (`produto`).
3. `python3 tools/squad/pending.py` antes → lista as 3 sem override; depois de cada `triage.py`, deixa de listar a triada.
4. `python3 tools/squad/triage.py <id> --runner claude` × 4, medindo o tempo de cada uma (CA3), com `claude` de verdade (sem mock).
5. Matriz completa de `POST /api/demand/start` e `/api/demand/clarify` (CA5/CA6), inspeção dos arquivos gerados em `docs/squad/inbox/*.json` (CA7).
6. Capturas: `console-1440`-equivalente `demandas-d4-1440.png` (fieldset/legend visíveis, tags de tipo, lista de demandas), `demandas-d4-1440-scroll.png` (card "Com perguntas" completo, botão Iniciar desabilitado + motivo + checkbox override, card "Iniciada · com override"), `demandas-d4-390.png` (coluna única na aba Demandas).

## Observações (não são defeitos de D4)

- A 390px, a barra de navegação superior (`nav`/header, fora do escopo desta demanda — o `git diff develop -- squad-control/index.html` não toca nenhuma regra de `nav`/`@media`) parece cortar itens à direita (ex.: "Política"/"Demandas" ficam fora da faixa visível sem scroll). Como o diff do D4 não mexeu no cabeçalho/CSS de navegação, isto é pré-existente e não foi introduzido por esta demanda — registrar como nota para o dono do painel (Frontend/Orquestrador), não como defeito de D4.
- O comentário da issue do GitHub por `validation`/`clarification` (`github_sync.py`) foi revisado no código mas não exercitado ao vivo (nenhum sync real disparado nesta rodada, para não criar issues de teste no repositório real).

## Defeitos encontrados

Nenhum defeito real. Implementação aderente ao contrato em todos os 10 critérios testados (CA1–CA10),
com evidência de API real, triagem real com o Claude Code e capturas reais em 1440/390px.
